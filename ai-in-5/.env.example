# AI in 5 — Email Configuration
# Copy this file to .env and fill in your details

# SMTP settings (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASS=your-app-password

# Sender details
FROM_NAME=AI Skills Passport
FROM_EMAIL=your.email@gmail.com
REPLY_TO=m.borck@curtin.edu.au

# Progress tracker API (for subscriber list)
API_URL=http://localhost:5050

# Start date for challenge cycling (YYYY-MM-DD)
# Day 1 of content-bank.csv will be sent on this date (should be a Monday)
# If not set, challenges cycle by weekday (Mon=1, Tue=2, etc.)
AI5_START_DATE=2026-03-02
